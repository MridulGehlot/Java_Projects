apply plugin: 'java'

sourceSets {
    main {
        java {
            srcDirs = ['src/main/java']
        }
    }
}

dependencies {
    implementation files('../bl/build/libs/bl.jar', '../netcommon/build/libs/netcommon.jar')
}
